# provider for passing miscellanious required data, when this gets less 'miscellanious'..
# split out into multiple more focused providers.
angular
  .module('impac.services.linking', [])
  .provider('impacLinking', () -> )
